# План — Михаил Борисович

Цель: после первоначальной настройки пользователь управляет ассистентом голосом.
Михаил выполняет локальные команды, Лариса разговаривает через Voice, Антон
принимает диктовку в текстовый ChatGPT и читает ответ. Далее — проверяемое
выполнение многошаговых просьб с помощью GPT, не запуск произвольного shell-кода.

## Снимок состояния
Сверен 24.09.2026: ветка `feature/chatgpt-voice`, HEAD
`4026c44c5b11f952811925a6087bb6408b284599` (`origin/feature/chatgpt-voice`).
Поверх checkpoint находятся незакоммиченные UX-правки LOCAL_MODE, склонения
времени, готовности/бюджета запуска Ларисы, ASR replay и atomic return. Существовавшие изменения документов сохранены. Основания и ограничения —
в [ACCEPTANCE.md](ACCEPTANCE.md).

## Сейчас: накопленный WIP готов к одному checkpoint
Функции в checkpoint-проходе не добавлялись. Финальный unit: 240 PASS / 0 FAIL /
8 opt-in SKIP; локальный browser fixture: 7 PASS / 0 FAIL / 0 SKIP; реальный corpus:
38 supported PASS / 0 FAIL. Пять повторов process-теста прошли; тестовых остатков нет.
Ограничения и разбор короткого test budget — в ACCEPTANCE. Commit выполняет
пользователь отдельным решением; следующий этап после него — короткий live regression.

24.09 пользователь подтвердил atomic cancel во время opening Антона и Ларисы,
а также глубокого mic startup Ларисы; очистка и LOCAL_MODE восстановлены.
Обе ветки mic readiness прошли живьём: off → один click → on и on без click,
затем PipeWire и LARISA_MODE. Это не новый conversation PASS.
LOCAL final/intent для Антона, Ларисы и времени корректны; отдельный final
«михаила» → NONE с успешным retry оставляет immediate readiness PARTIAL,
но не доказывает регрессию очереди. Склонение времени услышано корректным.

## Основания WIP и предшествующие проверки
Полный живой regression на HEAD `4026c44c5b11f952811925a6087bb6408b284599`
пройден: LOCAL_MODE, два вопроса Антону с RHVoice, возврат, разговор с Ларисой
и возврат, затем Ctrl+C через обычный `tee` с очисткой принадлежащих браузерных
процессов. Подробности и границы результата — в [ACCEPTANCE.md](ACCEPTANCE.md).

После review усилена граница локального wake-ответа: завершение плеера отмечается
одноразовым wait/event независимо от позднего poll главного цикла; PCM сравнивается
с этой границей по времени захвата PortAudio, а не доставки callback. Поздние TTS
и пересекающие границу блоки отбрасываются; полностью свежие не удаляются очисткой
очереди. Дополнительных 500 мс у wake-ответа нет, обычный/retry cooldown сохранён.
Это АВТО, не живой PASS; задержка планирования waiter и акустический хвост ещё
требуют проверки на машине. GPTModes в этом проходе не менялся. Склонение времени
и исправление батареи сохранены.

Последующий `live-final-20260923-191334.log` не закрыл immediate readiness:
первая команда времени прошла, следующая потребовала retry, после возврата
команда не была услышана. У каждого локального wake отмечен overflow=2.
Это был воспроизведённый дефект потери первых LOCAL блоков около speaking → listening.
Автоматически воспроизведён отказ от чтения очереди на протяжении wake-TTS,
затем устаревание пользовательского блока за очередью старого PCM. Теперь
просроченный префикс убирается и во время ожидания, а на переходе старый TTS
удаляется одним ограниченным проходом. Размер 12 и свежесть 400 мс сохранены.
Добавлена одна сводка LOCAL READY на переход. Полное исправление живого UX
не объявлено: конкретные задержки CPU/cleanup в том логе не измерялись.

Последующий live-ready выявил нестабильность старта Ларисы: два внешних timeout
и отдельный UIError при выборе Start Voice; во всех случаях cleanup подтверждён.
Причины задержки конкретных живых попыток не установлены. Подтверждённые дефекты
кода — немедленный отказ при ещё не готовой Voice-кнопке и конфликт внутренних
ожиданий с общим 90-секундным watchdog — исправлены. После review также запрещён
click Start Voice, если поиск вернулся после локального 30-секундного дедлайна;
общий startup budget 240 с сохранён. Прежняя ошибка unit-теста батареи исправлена
отдельно. Точные результаты и история автоматических прогонов — в ACCEPTANCE.
Закрытые задачи Bridge/SIGINT не возвращаются в активные.

В live-final Лариса открылась за 77,9 с и закрылась с подтверждённым
cleanup. Основная задержка наблюдалась до проверки PipeWire capture; бюджеты
и код Ларисы в проходе LOCAL queue не менялись. Разговор в этом запуске
новым PASS не считается.

Более поздний `live-local-ready-20260923-205611.log`: у LOCAL переходов overflow=0,
poll_lag≈1 мс, post_kept=0. Один immediate вызов не распознан, другой принят:
LOCAL остаётся PARTIAL. Сам final Vosk в старом логе отсутствует, причина отказа
неизвестна; ноль сохранённых на переходе блоков не является ошибкой. Очередь,
stale policy и playback boundary снова не менялись. Добавлена компактная
диагностика только LOCAL final/intent, включая NONE; partial и GPT-разговоры
не пишутся. Нужна короткая повторная серия с этой диагностикой.

В том же историческом запуске Лариса дошла до End Voice (68,5 с), затем microphone failed
на 99,9 с, до PipeWire не дошла; cleanup подтверждён. Это стало основанием
исправления готовности mic-контрола после End Voice; новый live результат указан выше. Автоисправление ждёт on/off
в прежнем общем 30-секундном окне стадии, допускает одну попытку включения и
ждёт подтверждённого mic_on. Неоднозначность и неподтверждённое включение дают
понятный отказ/cleanup. Общие budgets и PipeWire logic сохранены.

Ближайшая задача после автоматических проверок — короткий живой regression ниже.
LOCAL проверяется с final/intent diagnostic, Лариса — по структурным mic-стадиям
и подтверждённой очистке. Новая переделка LOCAL архитектуры
без воспроизводимого дефекта не планируется.
Автоисправление не означает новый live PASS.
LOCAL «Слушаю» fix сохранён и всё ещё ожидает окончательного живого acceptance:
команда сразу после последнего слога, без намеренной паузы, первое слово не теряется,
самопрослушивания и ложных команд нет.

## После checkpoint: короткий повторяемый live regression
Control replay использует production-классификацию. На внешнем corpus сначала
получено 35 supported PASS / 0 FAIL и 3 planned; после реализации atomic return
и явного обновления ожиданий трёх cases — 38 supported PASS / 0 FAIL.
WAV не менялись. Это ASR-проверка записей, не loop replay и не live acceptance.

Добавлен GPT-only возврат «Михаил вернись» / «Михаил Борисович вернись»:
не ждёт pending open/Send, использует существующий bounded cleanup. Самостоятельные
«Михаил» и «Вернись» не стали глобальными командами. Двухшаговый возврат сохранён;
control wake теперь ждёт final Vosk, чтобы partial-префикс не обрывал длинную atomic
фразу. Во время TTS/активного выхода/недостоверного monitor распознавание запрещено.
АВТО не подтверждает удобство endpoint-задержки на реальной машине.

Провести короткую серию, не benchmark:
1. LOCAL: три команды сразу после «Слушаю», без намеренной паузы; сохранить final/intent.
2. Atomic cancel: отдельно «Позови Антона Павловича» и «Позови Ларису»;
   во время opening сказать «Михаил вернись». Дождаться owned descendants reaped
   и LOCAL_MODE; новый вход только после завершения очистки.
3. Лариса: несколько полных startup до реального голосового ответа; проверить
   microphone-control → microphone-click (если нужен) → microphone-active,
   затем PipeWire. Старый mic FAIL не становится live PASS от АВТО.
4. Обычный двухшаговый возврат: «Михаил Борисович» → «Слушаю» → «Вернись».

LOCAL queue/stale/playback policy в этом проходе не менялась. Исправления
Bridge/SIGINT остаются закрытыми. Новый live PASS atomic и обеих веток mic readiness
получен по подтверждению пользователя 24.09; голосовой ответ в этих mic-runs
не подтверждён. Повторный короткий regression проверяет воспроизводимость.

Исторический полный unit-прогон имел один ошибочный process test с тестовым
budget 3 с. Подробный разбор этапа supervisor exit, изменение только test fixture
на production 12 с, повторные проверки и свежий полный результат — в ACCEPTANCE.
Production budgets и архитектура Bridge/SIGINT не меняются.

## Очередь этапов
| Этап | Результат / условие перехода |
|---|---|
| 1. Устойчивость ядра | Закрыт для дальнейшей разработки по текущим проверкам. Очередь, tick/monitor, Bridge и SIGINT/pipeline прошли автоматические проверки; полный живой regression на HEAD подтвердил возвраты и очистку. Один прогон не доказывает абсолютную надёжность. |
| 2. Проверенная v0.2.1 | После живой проверки старта Ларисы и LOCAL UX повторить полный живой regression; затем несколько повторов, прогон 30–60 минут, проверка ошибок и CPU/RAM/zram. Не переносить текущий одиночный PASS на длительную работу. |
| 3. Loop Replay Harness | Детерминированно подавать внешний PCM corpus в настоящий listen loop; timestamps, playback boundary, queue pressure и gap. ASR replay уже есть; runtime injection — отдельная следующая задача. |
| 4. Повседневный запуск | Звуковая готовность, понятные ошибки, один экземпляр, пользовательский автозапуск и восстановление; отдельная задача после regression и замеров. |
| 5. v0.3 — Agent Mode | Сначала GPT предлагает план без исполнения; затем 3–5 полезных сценариев из ограниченного набора инструментов. Валидация параметров, подтверждение чувствительных действий, проверка результата. |
| 6. Расширение | Заметки, документы, напоминания и другие функции по реальному использованию; отдельный инструмент и тесты для каждой. |

## Не блокировать друг другом
Подключение внешнего микрофона и колонок — отдельная аппаратная задача.
Оно нужно для итоговой оценки качества речи, но не для тестов очередей/процессов.
Причины задержек устанавливать измерением, не объявлять CPU или gain единственной
причиной по одному симптому. Не ждать идеального звука для проектирования агента;
зависания, случайные действия и повторные отправки остаются блокерами выпуска.

## Рабочий ритм
Один проход → тесты → ревью → нужная живая проверка → разрешённый checkpoint.
После доказанного результата закрывать этап; не оптимизировать без измеримого дефекта.
Не повторять старый benchmark: использовать `measure_resources.py`, время ответа
измерять отдельно. Передавать модели сводку и относящийся к ошибке фрагмент лога.

## Обновление
Держать здесь одну текущую задачу, следующий этап и условия готовности.
Результаты запусков — только в ACCEPTANCE. Большую историю не копировать.
